<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Praxis: A minimal habit tracker focusing on consistency and daily practice.">
    <meta name="theme-color" content="#F9F9F7">
    <title>Praxis | Daily Practice. Real Progress.</title>
    
    <style>
        /* --- CSS VARIABLES & RESET --- */
        :root {
            /* Palette: strictly no blue */
            --bg-color: #F9F9F7;
            --surface-color: #FFFFFF;
            --text-main: #2C2C2C;
            --text-muted: #6E6A66;
            --primary-color: #4A5D23; /* Dark Olive */
            --primary-hover: #3B4A1C;
            --border-color: #E6E4E0;
            --shadow-subtle: 0 2px 8px rgba(44, 44, 44, 0.04);
            
            /* Spacing & Type */
            --radius-md: 12px;
            --radius-lg: 20px;
            --font-main: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            --max-width: 1080px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-main);
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        /* --- TYPOGRAPHY --- */
        h1, h2, h3 {
            font-weight: 600;
            letter-spacing: -0.03em;
            line-height: 1.2;
        }

        p {
            color: var(--text-muted);
            font-size: 1.05rem;
        }

        a {
            text-decoration: none;
            color: inherit;
            transition: opacity 0.2s ease;
        }

        /* --- LAYOUT UTILITIES --- */
        .container {
            width: 90%;
            max-width: var(--max-width);
            margin: 0 auto;
            padding: 0 1rem;
        }

        section {
            padding: 5rem 0;
        }

        /* --- HEADER --- */
        header {
            padding: 2rem 0;
            background-color: var(--bg-color);
            position: sticky;
            top: 0;
            z-index: 10;
            backdrop-filter: blur(8px);
            /* Fallback for backdrop support */
            background-color: rgba(249, 249, 247, 0.95); 
        }

        .nav-wrapper {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .brand {
            display: flex;
            flex-direction: column;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            letter-spacing: -0.04em;
            color: var(--text-main);
        }

        .tagline {
            font-size: 0.75rem;
            color: var(--text-muted);
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
        }

        .nav-links li a {
            font-size: 0.9rem;
            color: var(--text-muted);
            font-weight: 500;
        }

        .nav-links li a:hover {
            color: var(--primary-color);
        }

        /* --- HERO SECTION --- */
        .hero {
            text-align: center;
            padding: 6rem 0;
            min-height: 70vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .hero-headline {
            font-size: clamp(2.5rem, 5vw, 4rem);
            margin-bottom: 1.5rem;
            color: var(--text-main);
        }

        .hero-subtext {
            max-width: 500px;
            margin: 0 auto 2.5rem;
            font-size: 1.2rem;
        }

        .cta-group {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            align-items: center;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: #fff;
            padding: 1rem 2rem;
            border-radius: var(--radius-md);
            font-weight: 600;
            font-size: 1rem;
            transition: transform 0.1s ease, background-color 0.2s;
        }

        .btn-primary:hover {
            background-color: var(--primary-hover);
            transform: translateY(-1px);
        }

        .btn-text {
            color: var(--text-main);
            font-weight: 500;
            border-bottom: 1px solid transparent;
        }

        .btn-text:hover {
            border-bottom: 1px solid var(--text-muted);
        }

        /* --- FEATURES SECTION --- */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background: var(--surface-color);
            padding: 2.5rem;
            border-radius: var(--radius-md);
            border: 1px solid var(--border-color);
            box-shadow: var(--shadow-subtle);
            transition: transform 0.2s ease;
        }

        .feature-card:hover {
            transform: translateY(-4px);
        }

        .feature-icon {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--primary-color);
            display: block;
        }

        .feature-title {
            font-size: 1.25rem;
            margin-bottom: 0.75rem;
        }

        /* --- AI SECTION (MIKA) --- */
        .ai-section {
            background-color: #EFECE9; /* Muted warm grey/brown */
            border-radius: var(--radius-lg);
            padding: 4rem 2rem;
            margin: 4rem 0;
            text-align: center;
        }

        .mika-badge {
            display: inline-block;
            background-color: var(--text-main);
            color: #fff;
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 0.05em;
            margin-bottom: 1.5rem;
        }

        .mika-quote {
            font-size: 1.5rem;
            font-weight: 500;
            color: var(--text-main);
            max-width: 600px;
            margin: 0 auto 1.5rem;
            font-style: italic;
        }

        .mika-desc {
            font-size: 1rem;
            max-width: 450px;
            margin: 0 auto;
        }

        /* --- FOOTER --- */
        footer {
            padding: 3rem 0;
            border-top: 1px solid var(--border-color);
            margin-top: 2rem;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: var(--text-muted);
            font-size: 0.85rem;
        }

        .footer-links {
            display: flex;
            gap: 1.5rem;
        }

        /* --- MOBILE RESPONSIVE --- */
        @media (max-width: 768px) {
            .hero-headline {
                font-size: 2.5rem;
            }
            
            .nav-links {
                display: none; /* Hidden for minimal mobile view as requested */
            }

            .cta-group {
                flex-direction: column;
                width: 100%;
            }

            .btn-primary {
                width: 100%;
                text-align: center;
            }

            .footer-content {
                flex-direction: column;
                gap: 1.5rem;
            }
        }
    </style>
</head>
<body>

    <header>
        <div class="container nav-wrapper">
            <div class="brand">
                <a href="#" class="logo">Praxis</a>
                <span class="tagline">Daily practice. Real progress.</span>
            </div>
            <nav>
                <ul class="nav-links">
                    <li><a href="#features">Features</a></li>
                    <li><a href="#ai">Mika AI</a></li>
                    <li><a href="#">Sign In</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <section id="hero" class="hero">
            <div class="container">
                <h2 class="hero-headline">Consistency beats<br>motivation.</h2>
                <p class="hero-subtext">
                    Motivation is fleeting. Habits are permanent. <br>
                    Track your daily execution with clinical precision.
                </p>
                <div class="cta-group">
                    <a href="#" class="btn-primary">Get Started</a>
                    <a href="#features" class="btn-text">Learn more &darr;</a>
                </div>
            </div>
        </section>

        <section id="features">
            <div class="container">
                <div class="features-grid">
                    <div class="feature-card">
                        <span class="feature-icon">&#9745;</span> <h3 class="feature-title">Daily Protocols</h3>
                        <p>Simple checklists for your daily habits. No streaks, no games. Just done or not done.</p>
                    </div>

                    <div class="feature-card">
                        <span class="feature-icon">&#9878;</span> <h3 class="feature-title">Linear Progress</h3>
                        <p>View your consistency over months and years. Data-driven reports designed for long-term optimization.</p>
                    </div>

                    <div class="feature-card">
                        <span class="feature-icon">&#9673;</span> <h3 class="feature-title">Accountability</h3>
                        <p>Automated check-ins that keep you honest. We focus on the system, not the goal.</p>
                    </div>
                </div>
            </div>
        </section>

        <section id="ai">
            <div class="container">
                <div class="ai-section">
                    <span class="mika-badge">MEET MIKA</span>
                    <blockquote class="mika-quote">
                        "I do not rely on how you feel today. I rely on what you planned to do. Let us execute."
                    </blockquote>
                    <p class="mika-desc">
                        Mika is your accountability partner. Calm, strict, and deeply supportive of your annual objectives.
                    </p>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container footer-content">
            <div class="copyright">
                &copy; 2024 Praxis. All rights reserved.
            </div>
            <div class="footer-links">
                <a href="#">Privacy Policy</a>
                <a href="#">Terms of Service</a>
            </div>
        </div>
    </footer>

</body>
</html>